# Required - use Neon pooled or direct connection string; SSL is required
DATABASE_URL=psql 'postgresql://neondb_owner:npg_aiXs7TrHGV1A@ep-royal-meadow-adphihn0-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'

# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=https://mpnag.vercel.app,http://localhost:5173,http://localhost:3000

# Optional: override port (Render sets PORT automatically in production)
PORT=3000
